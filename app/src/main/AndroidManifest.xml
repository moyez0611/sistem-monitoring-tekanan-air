<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.newtekanan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_pdam"
        android:label="Monitoring Tekanan"
        android:roundIcon="@mipmap/logo_pdam_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".spash_screen">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>

        <activity android:name=".Re"/>
        <activity android:name=".RecycleView" />
        <activity android:name=".DataTekanan" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>